function init(self)
	self.v = 0

	local url = msg.url("#test")
	rive.play_anim(url, "idle", go.PLAYBACK_LOOP_FORWARD)
end

function update(self, dt)
	self.v = self.v + dt

	local p = go.get_position()
	p.x = p.x + math.sin(self.v) * 4
	-- go.set_position(p)

	local r = vmath.quat_rotation_z(self.v * 0.1)
	-- go.set_rotation(r)
end

